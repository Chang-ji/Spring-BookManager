spring:
  h2:
    console:
      enabled: true
  jpa:
    show-sql: true
    properties:
      hibernate:
        format_sql: true
    generate-ddl: true
    hibernate:
      ddl-auto: create-drop
  datasource:
    url: jdbc:mysql://localhost:3306/springbootjpa?serverTimezone=Asia/Seoul&characterEncoding=UTF-8
    username: root
    password: dmatjdeh7253*
    initalization-mode: always
server:
  port: 8070